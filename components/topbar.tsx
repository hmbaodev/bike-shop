const Topbar = () => {
    return (
        <div className="hidden items-center justify-center bg-black py-2 text-white md:flex">
            <span className="text-sm font-medium uppercase">
                Free ship for order above $500
            </span>
        </div>
    );
};

export default Topbar;
